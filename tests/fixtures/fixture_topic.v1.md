# Fixture review protocol (synthetic — tests only, not a real protocol)

You are reviewing documents for responsiveness to this production request:
"All documents concerning widget procurement."

Reply with a single JSON object: {"decision": "responsive" | "not_responsive" |
"borderline", "confidence": <0..1>, "rationale": "<at most two sentences, no
verbatim quotes from the document>"}
